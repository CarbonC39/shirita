# M7 Plan 4 — 前端导入导出入口 + 包裹开关 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在定义库（BookView）接入导入入口（选 `.png,.json` + `on_conflict` + 结果摘要）、单定义/模板导出按钮，并在 DefinitionEditor 加 `wrap_in_tag` 开关。

**Architecture:** api 客户端新增 `importFile`（FormData → `POST /api/import`）与 `downloadExport`（带 Bearer 的 fetch → blob → `<a download>`，因鉴权走 header 不能直接 `window.open`）。DefinitionEditor 通过既有 `update:meta` 事件写 `meta.wrap_in_tag`。BookView 复用既有 Upload/Download 图标与确认对话框样式。

**Tech Stack:** Vue 3 `<script setup>` + TS + Vitest + lucide-vue-next。默认英文文案、无 emoji、禁 `v-html`。

**Upstream spec:** `docs/superpowers/specs/2026-06-16-m7-import-export-design.md`（§9 前端）。

---

## File Structure

- `shirita-ui/src/api/types.ts` — **modify**：`ImportSummary`/`ImportItem` 类型。
- `shirita-ui/src/api/client.ts` — **modify**：`importFile`、`downloadExport` + 两个导出 URL 助手。
- `shirita-ui/src/api/client.test.ts` — **create/extend**：`importFile` 构造 FormData 的单测。
- `shirita-ui/src/components/DefinitionEditor.vue` — **modify**：`wrap_in_tag` 开关。
- `shirita-ui/src/components/DefinitionEditor.test.ts` — **modify**：开关 emit 测试。
- `shirita-ui/src/views/BookView.vue` — **modify**：导入控件 + 导出按钮。

---

## Task 1: api 客户端 —— `importFile` + `downloadExport` + 类型

**Files:**
- Modify: `shirita-ui/src/api/types.ts`
- Modify: `shirita-ui/src/api/client.ts`
- Create: `shirita-ui/src/api/client.test.ts`（若已存在则追加）

- [ ] **Step 1: 加类型**

`shirita-ui/src/api/types.ts` 末尾加：

```ts
export interface ImportItem {
  kind: 'definition' | 'template'
  id: string
  name: string
}

export interface ImportSummary {
  created: ImportItem[]
  skipped: ImportItem[]
  overwritten: ImportItem[]
}

export type OnConflict = 'skip' | 'overwrite' | 'duplicate'
```

- [ ] **Step 2: 写失败测试**

创建（或追加）`shirita-ui/src/api/client.test.ts`：

```ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { importFile } from './client'

describe('importFile', () => {
  beforeEach(() => {
    vi.stubGlobal('fetch', vi.fn(async () =>
      new Response(JSON.stringify({ created: [], skipped: [], overwritten: [] }), { status: 200 }),
    ))
  })
  afterEach(() => vi.unstubAllGlobals())

  it('posts multipart FormData with file and on_conflict query', async () => {
    const file = new File([new Uint8Array([1, 2, 3])], 'card.png', { type: 'image/png' })
    await importFile(file, 'overwrite')
    const fetchMock = fetch as unknown as ReturnType<typeof vi.fn>
    const [url, init] = fetchMock.mock.calls[0]
    expect(String(url)).toContain('/api/import')
    expect(String(url)).toContain('on_conflict=overwrite')
    expect(init.method).toBe('POST')
    expect(init.body).toBeInstanceOf(FormData)
    expect((init.body as FormData).get('file')).toBeInstanceOf(File)
  })
})
```

- [ ] **Step 3: 跑测试看失败**

Run: `cd shirita-ui && npx vitest run src/api/client.test.ts`
Expected: FAIL（`importFile` 未导出）。

- [ ] **Step 4: 实现 `importFile` + `downloadExport` + URL 助手**

`shirita-ui/src/api/client.ts`：
1. 顶部 import 类型补 `ImportSummary, OnConflict`（合并进既有 `import type { ... } from './types'`）。
2. 文件末尾加：

```ts
export async function importFile(file: File, onConflict: OnConflict = 'skip'): Promise<ImportSummary> {
  const form = new FormData()
  form.append('file', file)
  const res = await fetch(`${BASE}/api/import?on_conflict=${onConflict}`, {
    method: 'POST',
    headers: authHeaders(), // 不要手动设 Content-Type：浏览器会带 boundary
    body: form,
  })
  if (!res.ok) throw new Error(`import failed: ${res.status}`)
  return res.json()
}

/// 带鉴权地拉取一个导出端点并触发浏览器下载（鉴权走 header，不能直接 window.open）。
export async function downloadExport(path: string, filename: string): Promise<void> {
  const res = await fetch(`${BASE}/api${path}`, { headers: authHeaders() })
  if (!res.ok) throw new Error(`export failed: ${res.status}`)
  const blob = await res.blob()
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = filename
  document.body.appendChild(a)
  a.click()
  a.remove()
  URL.revokeObjectURL(url)
}

export function exportDefinitionPath(id: string): string {
  return `/definitions/${id}/export`
}

export function exportTemplatePath(id: string): string {
  return `/templates/${id}/export`
}
```

- [ ] **Step 5: 跑测试看通过**

Run: `cd shirita-ui && npx vitest run src/api/client.test.ts`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add shirita-ui/src/api/types.ts shirita-ui/src/api/client.ts shirita-ui/src/api/client.test.ts
git commit -m "feat(ui): api client importFile + downloadExport for M7 import/export"
```

---

## Task 2: DefinitionEditor `wrap_in_tag` 开关

**Files:**
- Modify: `shirita-ui/src/components/DefinitionEditor.vue`
- Modify: `shirita-ui/src/components/DefinitionEditor.test.ts`

- [ ] **Step 1: 写失败测试**

在 `shirita-ui/src/components/DefinitionEditor.test.ts` 加一个用例（沿用文件既有的 mount 辅助/写法；下面给出独立可用版本，按既有 import 调整）：

```ts
import { mount } from '@vue/test-utils'
import { describe, it, expect } from 'vitest'
import DefinitionEditor from './DefinitionEditor.vue'
import type { Definition } from '../api/types'

function def(): Definition {
  return { id: 'd1', type: 'char', name: 'Alice', content: 'body', meta: {} }
}

describe('DefinitionEditor wrap_in_tag', () => {
  it('emits update:meta with wrap_in_tag toggled', async () => {
    const wrapper = mount(DefinitionEditor, {
      props: { definition: def(), allDefinitions: [] },
    })
    const box = wrapper.get('[data-test="wrap-in-tag"]')
    await box.setValue(true)
    const events = wrapper.emitted('update:meta')
    expect(events).toBeTruthy()
    const last = events![events!.length - 1][0] as Record<string, unknown>
    expect(last.wrap_in_tag).toBe(true)
  })
})
```

- [ ] **Step 2: 跑测试看失败**

Run: `cd shirita-ui && npx vitest run src/components/DefinitionEditor.test.ts`
Expected: FAIL（无 `[data-test="wrap-in-tag"]`）。

- [ ] **Step 3: 加开关到模板**

在 `DefinitionEditor.vue` 的设置区（参考既有 `update:meta` 用法，例如 trigger 那一带，约 168 行）加一段：

```vue
      <label class="flex items-center gap-2 text-sm text-ink">
        <input
          type="checkbox"
          data-test="wrap-in-tag"
          :checked="(definition.meta as Record<string, unknown>).wrap_in_tag === true"
          @change="emit('update:meta', { ...definition.meta, wrap_in_tag: ($event.target as HTMLInputElement).checked })"
        />
        Wrap content in a tag named after this definition
      </label>
```

> 与既有 meta 写法一致（展开 `...definition.meta` 再覆盖单键），不引入新 store/弹窗。文案英文（i18n 友好）。

- [ ] **Step 4: 跑测试看通过**

Run: `cd shirita-ui && npx vitest run src/components/DefinitionEditor.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add shirita-ui/src/components/DefinitionEditor.vue shirita-ui/src/components/DefinitionEditor.test.ts
git commit -m "feat(ui): DefinitionEditor wrap_in_tag toggle"
```

---

## Task 3: BookView 导入控件 + 导出按钮

**Files:**
- Modify: `shirita-ui/src/views/BookView.vue`

- [ ] **Step 1: 接 imports + 状态**

`BookView.vue` `<script setup>`：
1. 既有 `import { Check, Upload, Download, Copy, Trash2 } from "lucide-vue-next";` 已含 Upload/Download，无需改。
2. 从 client 引入新函数（合并进既有 `from "../api/..."` 导入块）：

```ts
import { importFile, downloadExport, exportDefinitionPath, exportTemplatePath } from "../api/client";
import type { OnConflict, ImportSummary } from "../api/types";
```

3. 加响应式状态（`<script setup>` 顶部状态区）：

```ts
const importConflict = ref<OnConflict>("skip");
const importSummary = ref<ImportSummary | null>(null);
const importBusy = ref(false);
const importInput = ref<HTMLInputElement | null>(null);

async function onImportPicked(e: Event) {
  const input = e.target as HTMLInputElement;
  const file = input.files?.[0];
  if (!file) return;
  importBusy.value = true;
  try {
    importSummary.value = await importFile(file, importConflict.value);
    await library.reload(); // 刷新定义/模板列表（见下注）
  } catch (err) {
    importSummary.value = null;
    console.error(err);
  } finally {
    importBusy.value = false;
    input.value = ""; // 允许再次选同一文件
  }
}

async function exportDefinition(d: Definition) {
  await downloadExport(exportDefinitionPath(d.id), `${d.name || "definition"}.json`);
}
```

> `library.reload()`：用 library store 既有的刷新方法重新拉取定义；若方法名不同（如 `load`/`fetchDefinitions`），按 `stores/library.ts` 实际命名调用。

- [ ] **Step 2: 加导入控件到模板**

在 BookView 模板的定义库区域顶部（靠近既有标题/操作栏）加：

```vue
    <div class="flex items-center gap-3 flex-wrap mb-3">
      <select v-model="importConflict" class="rounded border border-line bg-surface px-2 py-1 text-sm">
        <option value="skip">On conflict: skip</option>
        <option value="overwrite">On conflict: overwrite</option>
        <option value="duplicate">On conflict: duplicate</option>
      </select>
      <button
        type="button"
        class="inline-flex items-center gap-1 rounded border border-line px-2 py-1 text-sm hover:bg-surface"
        :disabled="importBusy"
        @click="importInput?.click()"
      >
        <Upload :size="16" /> Import
      </button>
      <input
        ref="importInput"
        type="file"
        accept=".png,.json,application/json,image/png"
        class="hidden"
        @change="onImportPicked"
      />
    </div>
    <p v-if="importSummary" class="text-sm text-muted mb-3">
      Imported: {{ importSummary.created.length }} created,
      {{ importSummary.skipped.length }} skipped,
      {{ importSummary.overwritten.length }} overwritten.
    </p>
```

- [ ] **Step 3: 加单定义导出按钮**

在定义行/编辑器操作区（既有 Copy/Trash2 按钮一带）加一个导出按钮：

```vue
        <button
          type="button"
          class="inline-flex items-center gap-1 rounded border border-line px-2 py-1 text-sm hover:bg-surface"
          @click="exportDefinition(definition)"
        >
          <Download :size="16" /> Export
        </button>
```

> 绑定的 `definition` 取自该区域既有的当前定义变量（BookView 里编辑中的定义对象）；若变量名不同（如 `editDef`），按实际调整。

- [ ] **Step 4: 构建校验（无 v-html、类型通过）**

Run: `cd shirita-ui && npx vue-tsc --noEmit && npx vitest run`
Expected: 类型通过；既有测试全绿。

> 若遇到 vite 缓存报错（`Failed to parse source for import analysis ... Install @vitejs/plugin-vue`），执行 `rm -rf node_modules/.vite node_modules/.vitest` 后重跑。

- [ ] **Step 5: 提交**

```bash
git add shirita-ui/src/views/BookView.vue
git commit -m "feat(ui): BookView import control + per-definition export"
```

---

## Task 4: 全量回归

- [ ] **Step 1: 后端 + 前端全绿**

Run:
```
cargo test --workspace
cd shirita-ui && npx vue-tsc --noEmit && npx vitest run && npx vite build
```
Expected: 全部通过、零警告、build 成功。

- [ ] **Step 2: 提交（如有 lockfile/构建产物变更则忽略 dist）**

无新增源文件时本步可空过；若 `npm` 锁文件变更则：

```bash
git add shirita-ui/package-lock.json
git commit -m "chore(ui): lockfile after M7 frontend"
```

---

## Self-Review Checklist

- **Spec 覆盖**：§9 导入入口（文件 `.png,.json` + `on_conflict` 选择 + 结果摘要）（Task3）✓、单定义导出按钮（Task3）✓、DefinitionEditor `wrap_in_tag` 开关（Task2）✓、模板导出（`exportTemplatePath` + `downloadExport` 助手已就绪，BookView 模板区如有模板导出入口可复用）✓；约束 无 emoji/英文文案/禁 v-html（全 Task）✓。
- **Placeholder 扫描**：无 TBD；api/组件/测试均完整代码。BookView 中 `library.reload()`/当前定义变量名注明"按实际命名调整"——属既有代码对接，非占位。
- **类型一致**：`importFile(File, OnConflict)->Promise<ImportSummary>`、`downloadExport(path,filename)`、`exportDefinitionPath(id)`/`exportTemplatePath(id)`、`ImportSummary{created,skipped,overwritten:ImportItem[]}` 全程一致，对齐 Plan 2/3 后端返回结构。
- **依赖前置**：依赖 Plan 2（`POST /api/import` + `ImportSummary` 形状）与 Plan 3（导出端点）。
```
